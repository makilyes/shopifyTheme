<link rel="stylesheet" href="{{ 'application.css' | asset_url }}" media="print" onload="this.media='all'">


<div class="container">
    <div class="collection">
      <h1 class="collection-title">{{ collection.title }}</h1>
      <div class="collection-list flex space-between">
        {% render 'product-card-collection' for collection.products as product %}
      </div>
    </div>
  </div>